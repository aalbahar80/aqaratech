{
  "name": "@self/site-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "tsc --noEmit --project tsconfig.json",
    "test": "playwright test",
    "prisma:cp": "cp -r ../prisma ./prisma",
    "prisma:gen": "pnpm run prisma:cp && prisma generate",
    "postinstall": "pnpm run prisma:gen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.21.1",
    "@prisma/client": "^3.13.0",
    "@self/seed": "workspace:^0.0.1",
    "@self/site": "workspace:^0.0.1",
    "cross-fetch": "^3.1.5",
    "dotenv": "^16.0.0",
    "prisma": "^3.13.0",
    "typescript": "^4.6.3"
  }
}