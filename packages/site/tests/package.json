{
  "name": "@self/site-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "tsc --noEmit --project tsconfig.json",
    "test:setup": "node ./db-setup.cjs",
    "test": "pnpm run test:setup && PLAYWRIGHT_EXPERIMENTAL_FEATURES=1 playwright test",
    "test:dev": "cross-env DEV=true pnpm run test:setup && PLAYWRIGHT_EXPERIMENTAL_FEATURES=1 playwright test",
    "prisma:gen": "prisma generate --schema=../prisma/schema.prisma",
    "postinstall": "pnpm run prisma:gen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "1.21.1",
    "@prisma/client": "3.14.0",
    "@self/seed": "workspace:0.0.1",
    "@self/site": "workspace:../package",
    "cross-env": "7.0.3",
    "dotenv": "16.0.1",
    "prisma": "3.14.0",
    "typescript": "4.6.4"
  }
}