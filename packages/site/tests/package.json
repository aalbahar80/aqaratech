{
  "name": "@self/site-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "tsc --noEmit --project tsconfig.json",
    "test:only": "playwright test -c non-auth; playwright test -c admin",
    "test:simul": "playwright test -c non-auth & playwright test -c admin",
    "test:setup": "node ./db-setup.js",
    "test": "pnpm run test:setup && pnpm run test:only",
    "test:dev": "cross-env DEV=true pnpm run test:setup && pnpm run test:only",
    "prisma:gen": "prisma generate --schema=../prisma/schema.prisma",
    "postinstall": "pnpm run prisma:gen"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.21.1",
    "@prisma/client": "^3.13.0",
    "@self/seed": "workspace:^0.0.1",
    "@self/site": "workspace:^0.0.1",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.0",
    "prisma": "^3.13.0",
    "typescript": "^4.6.3"
  }
}