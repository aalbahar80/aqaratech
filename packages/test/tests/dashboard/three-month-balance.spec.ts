/* eslint-disable @typescript-eslint/no-empty-function */
import { test } from '@playwright/test';

test.fixme('should display correct balance', async ({}) => {});

test.fixme(
	'total expenses is sum of properties and unspecified',
	async ({}) => {},
);
