mutation InsertTenant(
	$first_name: String
	$last_name: String
	$email: String
	$phone: String
) {
	insert_tenants_one(
		object: {
			first_name: $first_name
			last_name: $last_name
			email: $email
			phone: $phone
		}
	) {
		id
		first_name
		last_name
		email
		phone
	}
}
