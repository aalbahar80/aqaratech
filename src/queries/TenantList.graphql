query TenantList($limit: Int, $offset: Int, $order_by: [tenants_order_by!]!)
@cached {
	tenants(limit: $limit, offset: $offset, order_by: $order_by) {
		id
		first_name
		last_name
		email
		phone
	}
}
