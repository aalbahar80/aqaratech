query UnitList($limit: Int, $offset: Int, $order_by: [units_order_by!] = {})
@cached {
	units(order_by: $order_by, limit: $limit, offset: $offset) {
		id
		is_vacant
		rent_market
		size
		type
		unit_number
		usage
		bed
		bath
		floor
		property_id
		client_id_s
	}
}
