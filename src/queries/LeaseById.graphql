query LeaseById($id: Int!) {
	leases_by_pk(id: $id) {
		id
		start_date
		end_date
		is_expired
		is_signed
		monthly_rent
		license
		deposit
		tenant_id
		unit_id
		unit {
			id
			client_id_s
			property_id
		}
	}
}
