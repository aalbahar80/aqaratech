import type { Property } from '@prisma/client';
// export type IProperty = Property;
export type IProperty = Partial<Property>;
// export interface IProperty {
// 	area: string;
// 	// id: string;
// 	// clientId: string;
// }
