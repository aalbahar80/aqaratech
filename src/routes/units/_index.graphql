query UnitListPage(
	$limit: Int
	$offset: Int
	$order_by: [units_order_by!] = {}
	$where: units_bool_exp = {}
) {
	units(order_by: $order_by, limit: $limit, offset: $offset, where: $where) {
		...unitDetails
	}
	agg: units_aggregate(where: $where) {
		aggregate {
			count
		}
	}
}
