query ClientListPage(
	$limit: Int
	$offset: Int
	$order_by: [clients_order_by!] = {}
	$where: clients_bool_exp = {}
) {
	clients(order_by: $order_by, limit: $limit, offset: $offset, where: $where) {
		...clientDetails
	}
	agg: clients_aggregate(where: $where) {
		aggregate {
			count
		}
	}
}
