query LeasesScreen(
	$limit: Int
	$offset: Int
	$order_by: [leases_order_by!] = {}
	$where: leases_bool_exp = {}
) {
	leases(order_by: $order_by, limit: $limit, offset: $offset, where: $where) {
		...leaseDetails
	}
	agg: leases_aggregate(where: $where) {
		aggregate {
			count
		}
	}
}
