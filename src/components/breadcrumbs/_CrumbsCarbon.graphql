query TenantBreadcrumbs($id: Int!) {
	tenants_by_pk(id: $id) {
		id
		leases(order_by: { end_date: desc }, limit: 1) {
			id
			unit {
				id
				property {
					id
					client {
						id
					}
				}
			}
		}
	}
}
