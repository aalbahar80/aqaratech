name: CI

on:
  workflow_dispatch:
  push:
    tags:
      - "*"

jobs:
  build-dev:
    environment: dev
    uses: aqaratech/aqtech/.github/workflows/build.yml@master
    with:
      target: ${{ matrix.package }}
    secrets: inherit

  release-dev:
    uses: aqaratech/aqtech/.github/workflows/release.yml@master
    with:
      target: ${{ matrix.package }}
    secrets: inherit

    # # needs: test
    # strategy:
    #   fail-fast: false
    #   matrix:
    #     package: [site, backend]
    #     # environments blocked by sk's static/dynamic vars (for /site only)
    #     # environment: [dev, prod]

    # steps:
    #   - uses: actions/checkout@v3

    #   - name: Set .env file
    #     shell: bash
    #     env:
    #       AUTH0_CLIENT_SECRET: ${{ secrets.AUTH0_CLIENT_SECRET }}
    #     run: ./scripts/set-env.sh .env.dev .env AUTH0_CLIENT_SECRET

    #   - uses: aqaratech/aqtech/.github/workflows/build.yml@master
    #     with:
    #       target: ${{ matrix.package }}
    #     secrets: inherit

    # - uses: aqaratech/aqtech/.github/workflows/release.yml@master
    #   with:
    #     target: ${{ matrix.package }}
    #   secrets: inherit

    # - name: Create Github release
    #   uses: softprops/action-gh-release@v1
    #   with:
    #     prerelease: true

  # build-prod:
  #   runs-on: ubuntu-latest
  #   environment: prod
  #   # needs: test
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       package: [site, backend]
  #       # environments blocked by sk's static/dynamic vars (for /site only)
  #       # environment: [dev, prod]

  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Set .env file
  #       shell: bash
  #       env:
  #         AUTH0_CLIENT_SECRET: ${{ secrets.AUTH0_CLIENT_SECRET }}
  #       run: ./scripts/set-env.sh .env.prod .env AUTH0_CLIENT_SECRET

  #     - uses: aqaratech/aqtech/.github/workflows/build.yml@master
  #       with:
  #         target: ${{ matrix.package }}
  #       secrets: inherit

  #     - uses: aqaratech/aqtech/.github/workflows/release.yml@master
  #       with:
  #         target: ${{ matrix.package }}
  #       secrets: inherit

  #     - name: Create Github release
  #       uses: softprops/action-gh-release@v1
